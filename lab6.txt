เรื่อง pointer and Dynamic
1.Pointer
    1.1.Pointer and Addresses
    -ภาษา C เป็นตัวแปรที่ใช้เก็บค่าตำแหน่งหรือ address ในหน่วยความจำ
    -ค่าของตำแหน่งหรือ address ในหน่วยความจำก็คือค่าจำนวนเต็มบวกที่เริ่มจาก 0
    1.2.Pointer Operators
     -มีตัวดำเนินการ 2 ตัว
2.ฟังก์ชัน scanf() เป็นการรับข้อมูลเข้ามาในโปรแกรมผ่าน standard input
3.ความแตกต่างระหว่าง Pointers and Arrays อาเรย์จะสามารถเก็บข้อมูลประเภทเดียวกันใน1ตัวแปรได้
4.เราสามารถใช้ตัวแปร pointer อ้างไปยังตำแหน่งในหน่วยความจำ ของฟังก์ชันที่เราต้องการได้